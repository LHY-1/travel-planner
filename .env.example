# 设为 1 表示启用浏览器抓取；设为 0 表示完全关闭实时抓取，直接走 mock fallback
TONGCHENG_USE_BROWSER=1

# 设为 1 表示无头浏览器，不弹窗口；设为 0 表示可见浏览器
TONGCHENG_HEADLESS=1

# 浏览器登录态文件
TONGCHENG_STORAGE_STATE=browser_state/tongcheng-storage.json
